
ls | sort -r

   PIPE
   /   \
  ls   sort -r


ls -l | grep "file" | sort -r | wc -l

        PIPE
       /    \
     PIPE    WC
    /    \
  PIPE   SORT
  /   \
 LS  GREP
  |    |
 -l  "file"


     PIPE
    /    \
  LS    PIPE
  |    /    \
 -l  GREP   PIPE
            /   \
          SORT   WC
           |     |
          -r     -l


ls > file.txt

 REDIRECT_OUT
  /     \
 ls    file.txt


ls >> file.txt

 REDIRECT_APPEND
  /     \
 ls    file.txt


sort -r < file.txt

 REDIRECT_IN
  /        \
 sort -r  file.txt


cat < file.txt | sort -r > file2.txt

            PIPE
          /      \
 REDIRECT_IN     REDIRECT_OUT
  /        \       /        \
 cat  file.txt   sort -r  file.txt


ls -l | grep "file" && (cp file1 file2 || rm file3)

         AND
      /      \
   PIPE        OR
   /  \       /   \
  LS  GREP   CP    RM
 /      \    |       \
 -l   "file" file1 file2 file3

The command sequence lists files in the current directory,
searches for lines containing the word "file" in the output,
and then copies file1 to file2. If the copy is successful,
it does nothing more; otherwise, it removes file3.
